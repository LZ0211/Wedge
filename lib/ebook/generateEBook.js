"use strict";
const fs = require('fs');
const Stream = require('stream');

class Generator {
    constructor(input,format,output){
        this.input = input;
        this.format = format;
        this.output = output;
    }

    generate(){

    }

    use(){
        
    }
}